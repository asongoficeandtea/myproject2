---

- name: Add worker to manager node
  docker_swarm:
    state: join
    advertise_addr: worker
    join_token: "{{ hostvars['35.247.11.109']['swarm_info']['swarm_facts']['JoinTokens']['Worker'] }}"
    remote_addrs: [ 'manager-vm:2377' ]
